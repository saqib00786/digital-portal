import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Job = (props) => {
  return (
    <View>
      <Text>Job</Text>
    </View>
  );
};

export default Job;

const styles = StyleSheet.create({});
